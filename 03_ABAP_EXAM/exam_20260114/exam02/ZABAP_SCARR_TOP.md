```abap
*&---------------------------------------------------------------------*
*& Include          ZABAP_SCARR_TOP
*&---------------------------------------------------------------------*

TABLES: scarr, sscrfields.
" 사용자가 화면에서 버튼을 클릭하거나, 메뉴를 선택하거나, 엔터를 누를 때 발생하는 기능 코드(Function Code)가
" SSCRFIELDS-UCOMM 필드에 자동으로 저장.
" 특정 이벤트 블록(예: AT SELECTION-SCREEN 또는 AT USER-COMMAND) 내에서 이 구조체의 값을 읽어 사용자가 어떤 동작을 했는지 파악할 수 있다.
" 검색화면을 구성하는 구조  =>> 검색화면에서 클릭했을때 그 버튼 코드를 자동으로 받아올 수 있는 필드/ 버튼을 넣는 필드도 있다.


DATA: gt_scarr TYPE TABLE OF scarr,     " SCARR 불러오는 테이블
      gs_scarr TYPE scarr.              " LOOP돌릴때 필요한 행구조



" ALV 필수 변수
DATA: go_custom  TYPE REF TO cl_gui_custom_container,
      " go_custom  TYPE REF TO cl_gui_custom_container
      " ALV Grid와 같은 GUI 컨트롤을 ABAP 화면(Dynpro) 내에 배치할 수 있도록 영역(Area)을 정의하는 컨테이너 객체.
      " CL_GUI_CUSTOM_CONTAINER 레이아웃 버튼 눌러서 커스텀 컨트롤로 그린 화면을 받아오는 클래스
      " 개발자가 화면 레이아웃에서 특정 영역을 미리 정의하고, 프로그램 실행 시
      " 이 GO_CUSTOM 객체를 생성하여 그 영역을 ALV Grid를 담을 수 있는 '상자'로 만든다.

      go_grid    TYPE REF TO cl_gui_alv_grid. " ALV 출력하는 메소드가 들어가있는 클래스
      " 프로그램 실행 시 이 GO_GRID 객체를 생성하고, 이 객체가 위에서 생성된 GO_CUSTOM 컨테이너 안에 '쏙' 들어가서 데이터를 표시.
      " 데이터를 표 형태로 표시하고, 정렬, 필터링, 합계 등 ALV의 모든 표준 기능을 제공
*&---------------------------------------------------------------------*

" ALV Grid 객체(CL_GUI_ALV_GRID)를 사용하여 리포트를 출력할 때, 표의 형태와 레이아웃을 정의하는 데 사용되는 변수.
" LVC는 ALV Grid 컨트롤(CL_GUI_ALV_GRID)에서 사용되는 데이터 타입 집합.
DATA: gt_fcat   TYPE lvc_t_fcat,      " 필드 카탈로그 구조체의 표준 내부 테이블 타입. 필드 카탈로그 테이블.
      " ALV 리포트에 표시될 모든 열(Column)에 대한 정의 정보를 저장하는 메인 테이블
      " 이 테이블의 각 행은 ALV의 한 열을 나타냅니다.
      " 여기에 열 이름, 데이터 타입, 필드 라벨(열 제목), 출력 길이, 정렬 가능 여부, 숨김 여부 등 모든 메타데이터를 담게 됩니다.

      gs_fcat   TYPE lvc_s_fcat,      " 필드 카탈로그 작업 영역. 단일 필드 카탈로그 구조체. 필드카탈로그 행.
      " GT_FCAT 내부 테이블에 데이터를 추가하거나 수정하기 위한 작업 영역(Work Area)
      " 프로그램은 먼저 GS_FCAT의 필드(예: FIELDNAME, COLTEXT)에 원하는 열 정보를 채웁니다.
      " 정보를 모두 채운 후, 이 GS_FCAT를 APPEND 명령을 사용하여 GT_FCAT 테이블에 한 행으로 추가합니다.

      gs_layout TYPE lvc_s_layo.      " 레이아웃 구조체. 단일 레이아웃 구조체. 레이아웃 행
      " ALV Grid 전체의 전반적인 표시 설정을 정의하는 데 사용되는 구조체
      " 개별 열이 아닌 전체 표에 적용되는 설정들을 담는다.
      " ZEBRA: 행에 줄무늬(얼룩말 무늬)를 표시.
      " GRID_TITLE: ALV 표 상단에 표시될 제목.
      " NO_TOOLBAR: 툴바를 숨김
      " EDIT: ALV를 편집 모드로 설정
*&---------------------------------------------------------------------*


*&---------------------------------------------------------------------*
DATA: ok_code    TYPE sy-ucomm.
      " 사용자가 현재 화면에서 수행한 액션(기능 코드)을 저장.
      " 대화형 화면(Dynpro) 처리에서 SY-UCOMM의 값을 복사하여 저장하는 용도로 사용.
      " 화면의 로직 처리 중 SY-UCOMM 값이 변경될 수 있기 때문에,
      " 개발자들은 보통 화면 처리 시작 시 OK_CODE = SY-UCOMM.
      " 명령으로 현재 기능 코드를 OK_CODE에 보존해두고, 이후 로직에서 이 OK_CODE 변수만 참조하여 안정적으로 사용자 액션을 처리.

      " 현재 실행 중인 화면(Dynpro)에서 사용자가 PAI (Process After Input) 이벤트가 발생하도록 만든 기능 코드(Function Code)를 저장.

      " 1. 사용자가 ABAP 프로그램 화면에서 '저장' 버튼을 클릭합니다.
      " 2. 이 '저장' 버튼에 할당된 기능 코드(예: 'SAVE')가 SY-UCOMM 필드에 자동으로 채워집니다.
      " 3. SAP 시스템은 PAI 이벤트를 발생시키며 프로그램의 PAI 로직으로 이동합니다.
      " 4. ABAP 프로그램은 PAI 로직 내에서 IF SY-UCOMM = 'SAVE'. 와 같은 코드를 사용하여 사용자가 어떤 액션을 취했는지 판단하고,
      "    그에 맞는 저장 로직을 수행합니다.

      " 버튼 코드를 받아오는 부분 =>> 스크린 만들고 element list 두번째 탭 에 OK부분에 이걸 넣어야 버튼 코드를 받아온다.
*&---------------------------------------------------------------------*
DATA: gv_title   TYPE sy-title.
      " 프로그램이 특정 화면(Dynpro)을 처리하는 PAI (Process After Input) 이벤트나 PBO (Process Before Output) 이벤트에서,
      " 현재 화면에 설정된 제목을 GV_TITLE에 저장.
      " 예) GV_TITLE = SY-TITLE.
      " 현재 SAP GUI 창의 타이틀 바에 실제로 표시되고 있는 텍스트 값을 SY-TITLE 시스템 필드에서 읽어와서, 변수 GV_TITLE에 복사하여 저장.
      " 이는 나중에 제목을 다시 원래대로 되돌리기 위한 백업 용도로 사용된다.

      " 여기서는 로컬로 다시 선언해서 쓰이진 않았지만 타이틀을 넣는 구조
*&---------------------------------------------------------------------*
``