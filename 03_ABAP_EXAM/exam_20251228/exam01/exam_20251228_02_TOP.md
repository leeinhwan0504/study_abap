```abap
*&---------------------------------------------------------------------*
*& Include          Z03_TOP
*&---------------------------------------------------------------------*

* OLE
TYPE-POOLS: OLE2. " TYPES:   OLE2_OBJECT LIKE   OBJ_RECORD.  =>> 이 구조를 매크로 돌리기 위해서 사용했다.

* FUNCTION CODE
TABLES: SSCRFIELDS.
" 사용자가 화면에서 버튼을 클릭하거나, 메뉴를 선택하거나, 엔터를 누를 때 발생하는 기능 코드(Function Code)가
" SSCRFIELDS-UCOMM 필드에 자동으로 저장.
" 특정 이벤트 블록(예: AT SELECTION-SCREEN 또는 AT USER-COMMAND) 내에서 이 구조체의 값을 읽어 사용자가 어떤 동작을 했는지 파악할 수 있다.
" 검색화면을 구성하는 구조  =>> 검색화면에서 클릭했을때 그 버튼 코드를 자동으로 받아올 수 있는 필드/ 버튼을 넣는 필드도 있다.



DATA: G_FUNCTION_KEY TYPE SMP_DYNTXT. " 기능 키. 아이콘 버튼을 구성하는 구조
*&---------------------------------------------------------------------*

*EXCEL DOWN: DIRECTORY, SMW0
DATA: GV_DIRECTORY     TYPE STRING,   " 파일 저장 기본 경로. 샘플다운 버튼 눌렀을때 다운받을 경로를 받아오는 부분
      GV_INITIAL_DIR   TYPE STRING,   " 파일 저장/열기. 샘플다운 버튼 눌렀을때 다운받을 경로를 받아오는 부분

      " 다운로드할 파일의 이름 (파일명)을 저장하는 변수.
      " RLGRAP-FILENAME은 파일 경로를 저장하는 표준 ABAP 타입.
      GV_FILE          LIKE RLGRAP-FILENAME,  " 샘플다운 버튼 눌렀을때 저장할 전체경로를 넣어주는 부분

      " ABAP 프로그램이 사용자(클라이언트 PC)의 환경과 상호작용할 때 사용하는 표준 SAP 클래스.
      "  파일 처리: 클라이언트 PC에서 파일을 업로드하거나 다운로드합니다. (예: GUI_DOWNLOAD, GUI_UPLOAD)
      "  경로 및 환경 정보: 클라이언트 PC의 특정 폴더 경로(예: 바탕화면, 문서 폴더)를 가져오거나 파일의 존재 여부를 확인.
      "  시스템 제어: 클라이언트 PC에서 프로그램(예: Excel)을 실행하는 등의 작업을 수행.
      OBJFILE          TYPE REF TO CL_GUI_FRONTEND_SERVICES.    " 클래스를 참조해서 선언했다. =>> OBJFILE이 대신 받아서 클래스 메소드를 실행할 수 있게
*&---------------------------------------------------------------------*



*EXCEL DOWN: OLE
DATA: LS_KEY LIKE WWWDATATAB.   " OBJID라고 하는 CHAR 40 구조에 파일이름을 넣어주려고 사용했다.
" SAP Web Repository (SMW0)에 저장된 **바이너리 파일(예: Excel 템플릿) 을 가져올 때 사용되는 키 정보를 저장하는 로컬 구조체 변수.
" 파일의 MIME 타입, 이름 등을 저장

" OLE2_OBJECT 타입: ABAP이 클라이언트 PC의 Excel 애플리케이션과 통신할 때, Excel 내부의 특정 구성 요소(객체)를 가리키는 역할
DATA: GO_APPLICATION  TYPE OLE2_OBJECT,  " 엑셀 매크로 기능을 받는 구조. MS Excel 자체를 나타내는 최상위 객체. OLE 통신의 시작점이며, Excel을 실행하고 종료하는 데 사용.

      " GO_APPLICATION에 속한 모든 통합 문서(Workbook)들의 집합. 새 통합 문서를 열거나 추가하는 데 사용.
      GO_BOOKS        TYPE OLE2_OBJECT,  " Workbooks Collection.

      " 개별적인 통합 문서 파일 하나 (파일 열기/저장/닫기)
      GO_WBOOK        TYPE OLE2_OBJECT,  " Workbook. 엑셀 매크로 워크북 통합문서 기능을 받는다.
      GO_BOOK         TYPE OLE2_OBJECT,  " Workbook. 엑셀 매크로 워크북 통합문서 기능을 받는다.

      " 특정 GO_BOOK에 속한 모든 워크시트(Worksheet)들의 집합
      GO_SHEETS       TYPE OLE2_OBJECT,  " Sheets Collection

      " 개별적인 워크시트(시트 탭 하나) (시트 선택/활성화)
      GO_SHEET        TYPE OLE2_OBJECT,  " Worksheet. 엑셀 매크로 워크시트 기능을 받는다.

      " 워크시트 내의 개별 셀 또는 전체 셀 집합. 데이터 입력/읽기에 사용
      GO_CELLS        TYPE OLE2_OBJECT,  " Cells / Cell
      GO_CELL         TYPE OLE2_OBJECT,

      " 워크시트 내의 셀 범위(예: A1:C10). 서식 지정이나 블록 데이터 처리에 사용.
      GO_RANGE        TYPE OLE2_OBJECT,  " Range

      " 셀 또는 범위의 글꼴 속성. (글꼴 크기, 굵기 등 서식 제어)
      GO_FONT         TYPE OLE2_OBJECT,  " Font

      " 워크시트 내의 행
      GO_ROW          TYPE OLE2_OBJECT,  " Row

      GV_PATH         TYPE STRING,       " 다운로드할 Excel 파일의 전체 파일 경로
      GV_NUM          TYPE I.
*&---------------------------------------------------------------------*














" 기존
*DATA: BEGIN OF GS_ZSC,  " 오류검증하는 구조
*        ZSTATUS     TYPE ICON-ID,         " 아이콘
*        MANDT       TYPE ZSCARR-MANDT,
*        CARRID      TYPE ZSCARR-CARRID,
*        CARRNAME    TYPE ZSCARR-CARRNAME,
*        CURRCODE    TYPE ZSCARR-CURRCODE,
*        URL         TYPE ZSCARR-URL,
*        ZRESULT     TYPE CHAR200,         " 비고란
*      END OF GS_ZSC.
**&---------------------------------------------------------------------*
*
*
*DATA: BEGIN OF GS_EXCEL,  " 업로드하는 구조
*        MANDT       TYPE ZSCARR-MANDT,
*        CARRID      TYPE ZSCARR-CARRID,
*        CARRNAME    TYPE ZSCARR-CARRNAME,
*        CURRCODE    TYPE ZSCARR-CURRCODE,
*        URL         TYPE ZSCARR-URL,
*      END OF GS_EXCEL.
**&---------------------------------------------------------------------*

*&---------------------------------------------------------------------*
" 변경
DATA BEGIN OF GS_ZSC.             " 오류 검증하는 구조
  DATA ZSTATUS  TYPE ICON-ID.     " 아이콘
  INCLUDE TYPE  ZSTRAVELAG.       " ZSTRAVELAG 테이블의 타입을 가져온다.
  DATA ZRESULT  TYPE CHAR200.     " 비고란
DATA END OF GS_ZSC.

DATA: GS_EXCEL TYPE ZSTRAVELAG.         " 업로드하는 구조
*&---------------------------------------------------------------------*


DATA: GT_ZSC    LIKE TABLE OF GS_ZSC,   " 오류검증한 테이블
      GT_EXCEL  LIKE TABLE OF GS_EXCEL. " 업로드할 테이블
*&---------------------------------------------------------------------*


* 엑셀 업로드

" Excel 업로드 함수를 호출할 때, 업로드된 데이터를 담을 실제 내부 테이블 (예: GT_EXCEL)을 <gt_data>에 할당한다.
" 업로드할 데이터를 모르기때문에 모르는 거를 넣을 수 있는 구조를 사용 =>> 필드심볼
FIELD-SYMBOLS : <gt_data>       TYPE STANDARD TABLE .


" 여기 수정
* DB 데이터
*DATA: GT_ZSCARR TYPE TABLE OF ZSCARR,   " ZSCARR 불러오는 테이블
*      GS_ZSCARR TYPE          ZSCARR.   " LOOP돌릴때 필요한 행구조
*&---------------------------------------------------------------------*

*&---------------------------------------------------------------------*
" 수정
* DB 데이터
DATA: GT_TABLE TYPE TABLE OF ZSTRAVELAG,  " ZSTRAVELAG의 데이터를 불러오는 테이블
      GS_TABLE TYPE          ZSTRAVELAG.  " LOOP 돌릴때 필요한 행구조
*&---------------------------------------------------------------------*


* 기타
DATA: OK_CODE   TYPE SY-UCOMM,
      " 사용자가 현재 화면에서 수행한 액션(기능 코드)을 저장.
      " 대화형 화면(Dynpro) 처리에서 SY-UCOMM의 값을 복사하여 저장하는 용도로 사용.
      " 화면의 로직 처리 중 SY-UCOMM 값이 변경될 수 있기 때문에,
      " 개발자들은 보통 화면 처리 시작 시 OK_CODE = SY-UCOMM.
      " 명령으로 현재 기능 코드를 OK_CODE에 보존해두고, 이후 로직에서 이 OK_CODE 변수만 참조하여 안정적으로 사용자 액션을 처리.

      " 현재 실행 중인 화면(Dynpro)에서 사용자가 PAI (Process After Input) 이벤트가 발생하도록 만든 기능 코드(Function Code)를 저장.

      " 1. 사용자가 ABAP 프로그램 화면에서 '저장' 버튼을 클릭합니다.
      " 2. 이 '저장' 버튼에 할당된 기능 코드(예: 'SAVE')가 SY-UCOMM 필드에 자동으로 채워집니다.
      " 3. SAP 시스템은 PAI 이벤트를 발생시키며 프로그램의 PAI 로직으로 이동합니다.
      " 4. ABAP 프로그램은 PAI 로직 내에서 IF SY-UCOMM = 'SAVE'. 와 같은 코드를 사용하여 사용자가 어떤 액션을 취했는지 판단하고,
      "    그에 맞는 저장 로직을 수행합니다.

      " 버튼 코드를 받아오는 부분 =>> 스크린 만들고 element list 두번째 탭 에 OK부분에 이걸 넣어야 버튼 코드를 받아온다.

      GV_TITLE  TYPE SY-TITLE.
      " 프로그램이 특정 화면(Dynpro)을 처리하는 PAI (Process After Input) 이벤트나 PBO (Process Before Output) 이벤트에서,
      " 현재 화면에 설정된 제목을 GV_TITLE에 저장.
      " 예) GV_TITLE = SY-TITLE.
      " 현재 SAP GUI 창의 타이틀 바에 실제로 표시되고 있는 텍스트 값을 SY-TITLE 시스템 필드에서 읽어와서, 변수 GV_TITLE에 복사하여 저장.
      " 이는 나중에 제목을 다시 원래대로 되돌리기 위한 백업 용도로 사용된다.

      " 여기서는 로컬로 다시 선언해서 쓰이진 않았지만 타이틀을 넣는 구조
*&---------------------------------------------------------------------*

* ALV 관련
DATA  : GO_DOCKING  TYPE REF TO CL_GUI_DOCKING_CONTAINER,
*DATA : GO_CUSTOM TYPE REF TO CL_GUI_CUSTOM_CONTAINER,
       " ALV Grid와 같은 GUI 컨트롤을 ABAP 화면(Dynpro) 내에 배치할 수 있도록 영역(Area)을 정의하는 컨테이너 객체.
       " CL_GUI_CUSTOM_CONTAINER 레이아웃 버튼 눌러서 커스텀 컨트롤로 그린 화면을 받아오는 클래스
       " 개발자가 화면 레이아웃에서 특정 영역을 미리 정의하고, 프로그램 실행 시
       " 이 GO_CUSTOM 객체를 생성하여 그 영역을 ALV Grid를 담을 수 있는 '상자'로 만든다.

       " 프로그램 실행 시 이 GO_GRID 객체를 생성하고, 이 객체가 위에서 생성된 GO_CUSTOM 컨테이너 안에 '쏙' 들어가서 데이터를 표시.
       " 데이터를 표 형태로 표시하고, 정렬, 필터링, 합계 등 ALV의 모든 표준 기능을 제공
       GO_GRID TYPE REF TO CL_GUI_ALV_GRID.   " ALV 출력하는 메소드가 들어가있는 클래스
*&---------------------------------------------------------------------*

" ALV Grid 객체(CL_GUI_ALV_GRID)를 사용하여 리포트를 출력할 때, 표의 형태와 레이아웃을 정의하는 데 사용되는 변수.
" LVC는 ALV Grid 컨트롤(CL_GUI_ALV_GRID)에서 사용되는 데이터 타입 집합.
DATA : GT_FCAT   TYPE LVC_T_FCAT, " 필드 카탈로그 구조체의 표준 내부 테이블 타입. 필드 카탈로그 테이블.
       " ALV 리포트에 표시될 모든 열(Column)에 대한 정의 정보를 저장하는 메인 테이블
       " 이 테이블의 각 행은 ALV의 한 열을 나타냅니다.
       " 여기에 열 이름, 데이터 타입, 필드 라벨(열 제목), 출력 길이, 정렬 가능 여부, 숨김 여부 등 모든 메타데이터를 담게 됩니다.


       GS_FCAT   TYPE LVC_S_FCAT, " 필드 카탈로그 작업 영역. 단일 필드 카탈로그 구조체. 필드카탈로그 행.
       " GT_FCAT 내부 테이블에 데이터를 추가하거나 수정하기 위한 작업 영역(Work Area)
       " 프로그램은 먼저 GS_FCAT의 필드(예: FIELDNAME, COLTEXT)에 원하는 열 정보를 채웁니다.
       " 정보를 모두 채운 후, 이 GS_FCAT를 APPEND 명령을 사용하여 GT_FCAT 테이블에 한 행으로 추가합니다.



       GS_LAYOUT TYPE LVC_S_LAYO. " 레이아웃 구조체. 단일 레이아웃 구조체. 레이아웃 행
       " ALV Grid 전체의 전반적인 표시 설정을 정의하는 데 사용되는 구조체
       " 개별 열이 아닌 전체 표에 적용되는 설정들을 담는다.
       " ZEBRA: 행에 줄무늬(얼룩말 무늬)를 표시.
       " GRID_TITLE: ALV 표 상단에 표시될 제목.
       " NO_TOOLBAR: 툴바를 숨김
       " EDIT: ALV를 편집 모드로 설정
*&---------------------------------------------------------------------*

*툴바 관련
" ALV(ABAP List Viewer) 툴바의 특정 버튼이나 기능 코드를 제어(비활성화/숨김)하기 위해 사용
" ALV Grid 컨트롤은 기본적으로 많은 표준 기능(저장, 정렬, 합계 등)을 툴바에 제공합니다.
" 이 fcode 내부 테이블은 개발자가 표준 ALV 툴바에서 숨기거나 비활성화하고 싶은 기능 코드(버튼)들을 모아서 저장하는 데 사용됩니다.
" 툴바에서 '인쇄' 버튼을 숨기고 싶다면, '인쇄' 기능 코드(&PRINT 등)를 이 테이블에 추가.
DATA: fcode    TYPE TABLE OF sy-ucomm,    " 업로드 모드일때 버튼 코드 안나오게 하려고
      wa_fcode TYPE sy-ucomm.
*&---------------------------------------------------------------------*

" 여기 수정
*DATA: it_zscarrcp TYPE TABLE OF ZSCARR WITH HEADER LINE,
*      it_changes  TYPE TABLE OF ZSCARR WITH HEADER LINE,
*      it_deletes  TYPE TABLE OF ZSCARR WITH HEADER LINE.

" 수정
DATA: it_cp       TYPE TABLE OF ZSTRAVELAG WITH HEADER LINE,      " 복사하는 구조. 편집 전과 후를 비교하기 위해 복사해둘거다.
      it_changes  TYPE TABLE OF ZSTRAVELAG WITH HEADER LINE,      " 몇 개 변경저장했다를 메시지 출력하려고
      it_deletes  TYPE TABLE OF ZSTRAVELAG WITH HEADER LINE.      " 몇 개 삭자했다를 출력하려고
```